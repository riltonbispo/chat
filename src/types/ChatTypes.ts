export type ChatType = {
  chatId: string;
  title: string
  lastMessage: string
  messages: any
  lastMessageDate: any
  with: string
  date: any
  author: string
  body: string
};

export type UserType = {
  id: string
  email: string
  username: string
}