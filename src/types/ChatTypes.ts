export type ChatType = {
  chatId: number;
  title: string
  image: string
};

export type MessageType = {
  author: number
  body: string
}

export type UserType = {
  id: number
  avatar: string
  name: string
}